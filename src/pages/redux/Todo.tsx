import { Box, Typography } from "@mui/material";

const Todo = () => {
  return (
    <Box
      display="flex"
      justifyContent="center"
      height="100vh"
      alignItems="center"
    >
      <Typography variant="h4"> Todo</Typography>
    </Box>
  );
};

export default Todo;
